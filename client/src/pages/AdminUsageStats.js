import { useEffect,useState } from "react";import { Link } from "react-router-dom";const API="https://alonpc02026.onrender.com/api/usage-stats/summary";export default function AdminUsageStats(){const[days,setDays]=useState(1),[data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{setData(null);fetch(`${API}?days=${days}`).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.message||"שגיאה");return d}).then(setData).catch(e=>setError(e.message))},[days]);return <main dir="rtl" style={{maxWidth:1050,margin:"0 auto",padding:20}}><h1>📊 סטטיסטיקת האתר</h1><div style={{display:"flex",gap:10}}><button onClick={()=>setDays(1)}>היום</button><button onClick={()=>setDays(7)}>7 ימים</button><button onClick={()=>setDays(30)}>30 ימים</button><Link to="/admin">חזרה לניהול</Link></div>{error&&<p>❌ {error}</p>}{!data&&!error&&<p>טוען נתונים...</p>}{data&&<><p><b>👥 כניסות:</b> {data.visits}</p><p><b>👆 סך לחיצות:</b> {data.totalClicks}</p><h2>לאיזה תחום נכנסו?</h2>{data.clicks.map(x=><div key={x.key} style={{display:"flex",justifyContent:"space-between",border:"2px solid #4676a8",borderRadius:10,padding:12,margin:"8px 0"}}><span>{x.label||x.key}</span><strong>{x.count}</strong></div>)}</>}</main>}
